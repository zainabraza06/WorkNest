APP_ENV=development
# Comma-separated origins allowed to call this service (normally only the backend, server-to-server)
ALLOWED_ORIGINS=http://localhost:5000
# Optional shared secret; if set, backend must send it as the X-Service-Key header
SERVICE_API_KEY=
