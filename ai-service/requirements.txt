fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# ── Models (needed to serve real predictions and to retrain) ──
scikit-learn>=1.5
pandas>=2.2
joblib>=1.4

# ── Semantic search: local ONNX embeddings, no torch and no LLM API ──
# Downloads ~130MB of model weights to ~/.cache on first use; falls back to lexical without it.
fastembed>=0.4
